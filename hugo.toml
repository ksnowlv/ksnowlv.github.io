baseURL = 'https://ksnowlv.github.io'
languageCode = 'en-us'
theme = 'mainroad'
paginate = "12" # Number of posts per page

publishDir = "docs"

[Author]
  name = "John Doe"
  bio = "John Doe's true identity is unknown. Maybe he is a successful blogger or writer. Nobody knows it."
  avatar = "img/avatar.png"

[Params]
  description = "John Doe's Personal blog about everything" # Description of your site
  opengraph = true
  twitter_cards = false
  readmore = false # Show "Read more" button in list if true
  authorbox = true
  pager = true
  post_meta = ["date", "categories"] # Order of post meta information
  mainSections = ["post", "docs"]

[Params.logo]
  title = 'ksnowlv的技术笔记'
  subtitle = "立足现在,铭记当下" # Logo subtitle

[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = true # Enable counter for each tag in "Tags" widget (disabled by default)
  categories_counter = true # Enable counter for each category in "Categories" widget

# 归档页面菜单项
#[[menu.main]]
#    identifier = "archives"
#    name = "文章归档"
#    url = "/docs/archives"
#    weight = 2

# 归档页面路径和布局文件
#[[module.mounts]]
#   source = "layouts/shortcodes/postref.html"
#   target = "layouts/shortcodes/postref.html"

#[[module.mounts]]
#   source = "content/post/"
#   target = "content/post/"